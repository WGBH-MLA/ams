<% holding_organization =  OrganizationService.new %>
<%= f.input :holding_organization, as: :select,
            collection: holding_organization.select_all_options,
            include_blank: true,
            disabled:f.object.respond_to?(:disabled?) && f.object.disabled?(key),
            input_html: { class: 'form-control'}
%>