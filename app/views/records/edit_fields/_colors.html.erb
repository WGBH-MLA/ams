<% colors_service =  ColorsService.new %>
<%= f.input :colors, as: :select,
            collection: colors_service.select_all_options,
            include_blank: true,
            disabled:f.object.respond_to?(:disabled?) && f.object.disabled?(key),
            input_html: { class: 'form-control'}
%>