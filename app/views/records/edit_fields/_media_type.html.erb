<%= f.input :media_type, as: :select,
            collection: MediaTypeService.select_all_options,
            include_blank: true,
            disabled:f.object.respond_to?(:readonly?) && f.object.readonly?(key),
            input_html: { class: 'form-control'}
%>