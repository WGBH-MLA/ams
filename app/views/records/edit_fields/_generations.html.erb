<% generations_service = GenerationsService.new %>
<%= f.input :generations, as: :select,
            collection: generations_service.select_all_options,
            include_blank: true,
            disabled:f.object.respond_to?(:readonly?) && f.object.readonly?(key),
            input_html: { class: 'form-control' , multiple: true}
%>