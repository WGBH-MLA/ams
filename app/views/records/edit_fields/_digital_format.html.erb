<%= f.input :digital_format, as: :select,
            collection: DigitalFormatService.select_all_options,
            include_blank: true,
            disabled:f.object.respond_to?(:readonly?) && f.object.readonly?(key),
            input_html: { class: 'form-control'}
%>
