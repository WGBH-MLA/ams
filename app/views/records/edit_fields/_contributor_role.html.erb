<% contributor_role_service =  ContributorRoleService.new %>
<%= f.input :contributor_role, as: :select,
            collection: contributor_role_service.select_all_options,
            include_blank: true,
            disabled:f.object.respond_to?(:readonly?) && f.object.readonly?(key),
            input_html: { class: 'form-control' , multiple: false}
%>